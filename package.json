{
  "name": "@cewald/website",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "prepare": "husky && nuxi prepare",
    "build": "nuxt build",
    "dev": "nuxt dev --no-fork",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "npm run prepare",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "typecheck": "npx nuxi typecheck"
  },
  "devDependencies": {
    "@cewald/nuxt-boilerplate-layer": "^7.1.1",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.0.1",
    "@vueuse/core": "^14.4.0",
    "gsap": "^3.15.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "nuxt": "^4.5.1",
    "vue": "^3.5.33",
    "vue-router": "^5.2.0"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown",
      "npm run lint:fix"
    ]
  }
}
