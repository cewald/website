{
  "name": "@cewald/website",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "prepare": "husky && nuxi prepare",
    "build": "nuxt build",
    "dev": "nuxt dev --no-fork",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "npm run prepare",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "typecheck": "npx nuxi typecheck"
  },
  "devDependencies": {
    "@cewald/nuxt-boilerplate-layer": "^5.3.7",
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@vueuse/core": "^13.9.0",
    "gsap": "^3.13.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.5",
    "nuxt": "^4.1.3",
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown",
      "npm run lint:fix"
    ]
  }
}
